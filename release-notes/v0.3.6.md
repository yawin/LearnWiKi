## ✨ What's New

- Fixed in-app auto-update so new versions can install themselves again.
- When auto-update isn't available, the update banner now opens the download page automatically so you can grab the new version manually.

## Installation (macOS)

1. Download the `.dmg` file that matches your Mac:
   - **Apple Silicon (M1/M2/M3/M4)**: `LearnWiki_0.3.6_aarch64.dmg`
   - **Intel Mac**: `LearnWiki_0.3.6_x64.dmg`
2. Double-click the `.dmg` and drag **LearnWiki** into the **Applications** folder.
3. Double-click LearnWiki to launch — no first-run security prompt.
4. Go to **Settings → AI** to configure your AI provider (Claude / OpenAI / Gemini).

---

## ✨ 本次更新

- 修复了应用内自动升级失效的问题，新版本现在可以正常自动更新。
- 当自动升级暂时不可用时，升级提示会自动打开下载页，方便手动下载新版本。

## 安装说明（macOS）

1. 下载与你的 Mac 对应的 `.dmg` 文件：
   - **Apple Silicon（M1/M2/M3/M4）**：`LearnWiki_0.3.6_aarch64.dmg`
   - **Intel 芯片 Mac**：`LearnWiki_0.3.6_x64.dmg`
2. 双击 `.dmg`，将 **LearnWiki** 拖入 **Applications**（应用程序）文件夹。
3. 双击 LearnWiki 即可打开，**不会再有首次启动的安全提示**。
4. 打开应用后进入 **设置 → AI**，填入 AI 服务商的 API Key（Claude / OpenAI / Gemini）。
