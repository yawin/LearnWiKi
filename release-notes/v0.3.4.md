## ✨ What's New

- In-app auto-update — when a new release is available, click **Update now** in the banner to download, verify, install, and relaunch in one step. No more bouncing to GitHub to grab a DMG manually. (Takes effect from v0.3.4 onward — once you're on this build, future versions arrive in-app.)

## Installation (macOS)

1. Download the `.dmg` file that matches your Mac:
   - **Apple Silicon (M1/M2/M3/M4)**: `LearnWiki_0.3.4_aarch64.dmg`
   - **Intel Mac**: `LearnWiki_0.3.4_x64.dmg`
2. Double-click the `.dmg` and drag **LearnWiki** into the **Applications** folder.
3. Double-click LearnWiki to launch — no first-run security prompt.
4. Go to **Settings → AI** to configure your AI provider (Claude / OpenAI / Gemini).

---

## ✨ 本次更新

- 应用内一键升级 —— 有新版本时，顶部横幅会出现「立即升级」按钮，点一下就自动下载、校验、安装并重启，再也不用跳到 GitHub 手动下 DMG。（从 v0.3.4 起生效，装上这个版本后，以后的新版本都能在 App 里直接升级。）

## 安装说明（macOS）

1. 下载与你的 Mac 对应的 `.dmg` 文件：
   - **Apple Silicon（M1/M2/M3/M4）**：`LearnWiki_0.3.4_aarch64.dmg`
   - **Intel 芯片 Mac**：`LearnWiki_0.3.4_x64.dmg`
2. 双击 `.dmg`，将 **LearnWiki** 拖入 **Applications**（应用程序）文件夹。
3. 双击 LearnWiki 即可打开，**不会再有首次启动的安全提示**。
4. 打开应用后进入 **设置 → AI**，填入 AI 服务商的 API Key（Claude / OpenAI / Gemini）。
