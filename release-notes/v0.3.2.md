## ✨ What's New

- The app is now signed and notarized by Apple — double-clicking the DMG no longer shows the "LearnWiki cannot be opened because Apple cannot check it for malicious software" warning. Just install and run.

## Installation (macOS)

1. Download the `.dmg` file that matches your Mac:
   - **Apple Silicon (M1/M2/M3/M4)**: `LearnWiki_0.3.2_aarch64.dmg`
   - **Intel Mac**: `LearnWiki_0.3.2_x64.dmg`
2. Double-click the `.dmg` and drag **LearnWiki** into the **Applications** folder.
3. Double-click LearnWiki to launch — no first-run security prompt anymore.
4. Go to **Settings → AI** to configure your AI provider (Claude / OpenAI / Gemini).

---

## ✨ 本次更新

- 应用已通过 Apple 官方签名和公证 —— 安装时不会再出现「无法打开 LearnWiki，Apple 无法验证 是否包含恶意软件」的警告，下载后双击即可运行。

## 安装说明（macOS）

1. 下载与你的 Mac 对应的 `.dmg` 文件：
   - **Apple Silicon（M1/M2/M3/M4）**：`LearnWiki_0.3.2_aarch64.dmg`
   - **Intel 芯片 Mac**：`LearnWiki_0.3.2_x64.dmg`
2. 双击 `.dmg`，将 **LearnWiki** 拖入 **Applications**（应用程序）文件夹。
3. 双击 LearnWiki 即可打开，**不会再有首次启动的安全提示**。
4. 打开应用后进入 **设置 → AI**，填入 AI 服务商的 API Key（Claude / OpenAI / Gemini）。
